# ✅ Исправление применено!

## Что было исправлено:

1. **API URL конфигурация** - теперь используется относительный путь `/api` в Docker
2. **Nginx прокси** - правильно настроен для проксирования запросов к backend

## 🔄 Что нужно сделать:

**Пересоберите frontend контейнер:**

```bash
docker-compose up -d --build frontend
```

Или пересоберите все:

```bash
docker-compose up -d --build
```

## ✅ После пересборки:

1. Откройте http://localhost:3000
2. Откройте консоль браузера (F12)
3. Проверьте, что запросы к API работают (Network tab)
4. Попробуйте создать тему в разделе "Темы"

## 🐛 Если все еще не работает:

1. Проверьте логи: `docker logs flashcard-frontend`
2. Проверьте, что backend доступен: `curl http://localhost:8080/api/topics`
3. Откройте консоль браузера и пришлите ошибки

## 💡 Для локальной разработки (без Docker):

```bash
# Backend
cd backend
./gradlew bootRun

# Frontend (в другом терминале)
cd frontend
npm install
npm run dev
```

В dev режиме будет использоваться `http://localhost:8080/api` автоматически.

