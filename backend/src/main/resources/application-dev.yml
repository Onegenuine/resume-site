spring:
  datasource:
    url: jdbc:h2:mem:flashcarddb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  h2:
    console:
      enabled: true
      path: /h2-console
  
  jpa:
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    com.flashcard: DEBUG

