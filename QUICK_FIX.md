# Быстрое решение проблем

## ✅ Статус сервисов

Судя по проверке:
- ✅ Docker контейнеры запущены
- ✅ Backend доступен на http://localhost:8080
- ✅ Frontend доступен на http://localhost:3000
- ✅ API отвечает (возвращает [])

## 🔍 Что проверить в браузере

1. **Откройте приложение:** http://localhost:3000

2. **Откройте DevTools (F12)** и проверьте:
   - **Console** - есть ли ошибки (красные сообщения)
   - **Network** - работают ли запросы к `/api/topics`, `/api/questions`

3. **Проверьте запросы к API:**
   - В Network найдите запрос к `http://localhost:8080/api/topics`
   - Проверьте статус ответа (должен быть 200)
   - Проверьте CORS заголовки

## 🐛 Частые проблемы и решения

### Проблема: "Failed to fetch" или "Network Error"

**Решение 1:** Проверьте, что backend доступен:
```bash
curl http://localhost:8080/api/topics
# Должен вернуть: []
```

**Решение 2:** Если используете Docker, проверьте логи:
```bash
docker logs flashcard-backend
docker logs flashcard-frontend
```

**Решение 3:** Перезапустите контейнеры:
```bash
docker-compose restart
```

### Проблема: "CORS error"

Backend уже настроен с `@CrossOrigin(origins = "*")`, но если проблема есть:
1. Проверьте, что запросы идут на правильный URL
2. В `frontend/src/services/api.js` должен быть: `http://localhost:8080/api`

### Проблема: Страница белая или ничего не отображается

1. Откройте консоль браузера (F12)
2. Проверьте ошибки JavaScript
3. Проверьте, что все файлы загружаются (Network tab)

### Проблема: Блоки на главной странице не кликаются

1. Проверьте консоль на ошибки
2. Убедитесь, что `react-router-dom` установлен
3. Проверьте, что используется правильная версия React Router

## 🚀 Полная перезагрузка

Если ничего не помогает:

```bash
# Остановить все
docker-compose down

# Удалить volumes (ОСТОРОЖНО: удалит данные!)
# docker-compose down -v

# Пересобрать и запустить
docker-compose up -d --build

# Проверить логи
docker logs flashcard-backend -f
docker logs flashcard-frontend -f
```

## 📝 Что должно работать

1. **Главная страница** (/) - большие блоки с переходами
2. **Темы** (/topics) - список тем, создание, редактирование
3. **Вопросы** (/questions) - список вопросов, создание, загрузка из файла
4. **Повторение** (/review) - повторение материала

## 💡 Если проблема не решена

Пришлите:
1. Скриншот консоли браузера (F12 → Console)
2. Скриншот Network tab (F12 → Network)
3. Вывод команды: `docker logs flashcard-backend --tail 50`

